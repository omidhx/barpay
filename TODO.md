# TODO — نقشه اجرایی barnameh-pay (فازهای ۰ تا ۷)

> برآورد کلی: ~۱۶ تا ۲۳ هفته (توسعه انفرادی با کمک AI). این برآورد است، نه تعهد.
> ترتیب شروع: **ابتدا مدل داده، Import و ماشین وضعیت** — نه احراز هویت (فصل ۱۴.۵).

## فاز ۰ — تثبیت قواعد (۳–۵ روز) ⬜

- [ ] چهار فایل اجرایی review و تثبیت: business-rules / data-model / state-transitions / invariants
- [ ] **Artifactهای بیرونی (Definition of Ready):**
  - [ ] متن رسمی تعهدنامه با تأیید مشاور حقوقی → تعیین‌کنندهٔ فعال‌سازی ENFORCED
  - [ ] فایل اکسل نمونهٔ شرکت اول → تنظیم `excel_column_mappings` + تست import واقعی
  - [ ] تأیید قالب‌های پیامک روی خط واقعی (فراز + ملی‌پیامک)
  - [ ] مطالعهٔ داکیومنت رسمی فراز/ملی‌پیامک و قفل endpointها در [sms-providers](docs/integrations/sms-providers.md)
  - [ ] مطالعهٔ کامل مستندات رسمی ۶ درگاه و قفل جزئیات در [payment-gateways](docs/integrations/payment-gateways.md)
  - [ ] انتخاب/قرارداد درگاه + مدارک (ای‌نماد، کد رهگیری مالیاتی)
- [ ] دادهٔ تست anonymized (هرگز دادهٔ واقعی در ریپو)

**شرط عبور:** هیچ ابهام مالی مسدودکننده‌ای باقی نمانَد.

## فاز ۱ — طراحی تجربه و فنی (۴–۶ روز) ⬜

- [ ] وایرفریم صفحات پنل (۱۷ صفحه — فهرست در [ui-ux-rtl-guidelines](docs/frontend/ui-ux-rtl-guidelines.md))
- [ ] وایرفریم پورتال راننده (مسیر ۴ گامی: اطلاعات → تعهدنامه → پرداخت → دریافت)
- [ ] مدل داده نهایی + ماشین وضعیت + مدل تهدید

**شرط عبور:** مرور یک سناریوی کامل با کاربر واقعی.

## فاز ۲ — پایه پروژه (۴–۶ روز) ⬜

- [ ] ریپو `barnameh-pay` (Private) + CI (lint/typecheck/test/drift-check)
- [ ] Docker Compose (web + worker از یک ایمیج، دو سرویس) + nginx
- [ ] احراز هویت پنل، نقش‌ها، مجوزهای مستقل، نشست DB-backed
- [ ] staging + **اولین بکاپ و آزمون restore** (از همین فاز — نه فاز ۷)

**شرط عبور:** استقرار خودکار + تست مجوز پایه + بکاپ/restore آزموده.

## فاز ۳ — اکسل و اسناد (۱–۲ هفته) ⬜

- [x] Import کامل ۱۱ گامی + پیش‌نمایش + اعتبارسنجی + عبور ردیف صفر/صفر
- [ ] job پس‌زمینه برای فایل >۵۰۰ ردیف (COMMITTING)
- [x] بارگذاری گروهی PDF + تطبیق خودکار از نام فایل + اتصال دستی
- [x] سیاست ۱:۱ (حداکثر یک PDF فعال به‌ازای بارنامه)

**شرط عبور:** سناریوی ۵۰۰ ردیف / ۵۰۰ PDF بدون خطا.

## فاز ۴ — پورتال راننده و تعهدنامه (۱–۲ هفته) ✅

- [x] لینک کوتاه‌عمر (توکن هش‌شده) + OTP با همهٔ محدودیت‌ها
- [x] صفحات موبایل‌محور راننده + **گام تعهدنامه و امضا پیش از پرداخت**
- [x] سه حالت OFF/SHADOW/ENFORCED (flag `COMMITMENT_ENFORCEMENT`)
- [x] پیامک دو سرویسی + سوئیچ خودکار + مدارشکن + SLA ۳۰ ثانیه

**شرط عبور:** رانندهٔ واقعی بدون نصب، مسیر تعهدنامه را کامل کند؛ رد پذیرش = توقف.

## فاز ۴ب — درگاه پرداخت و کارت‌ها (۱–۲ هفته) ⬜

- [ ] آداپتر واحد `PaymentProvider` + شش ارائه‌دهنده (سامان/ملت/پاسارگاد/سداد/زرین‌پال/زیبال)
- [ ] endpoint شروع پرداخت + callback عمومی + **وریفای server-to-server**
- [ ] مقایسهٔ مبلغ، UNKNOWN با job استعلام، انقضای تلاش ۲۰ دقیقه
- [ ] ماژول کارت‌های واریز: CRUD + Luhn/شبا + نمایش گرافیکی کپی‌پذیر
- [ ] پنل پیکربندی درگاه + «تست اتصال»

**شرط عبور:** چرخهٔ کامل sandbox + callback جعلی/تکراری رد شود.

## فاز ۵ — بررسی و آزادسازی (۱–۲ هفته) ⬜

- [ ] صف بررسی پرداخت‌های دستی + تأیید/رد/مغایرت با دلیل
- [ ] `canRelease` مرکزی + گرنت اتمیک دانلود
- [ ] اصلاح مبلغ + لینک مابقی + ثبت بازگشت وجه با تأیید سرپرست
- [ ] رویدادهای مالی کامل در audit_logs (از همین فاز)

**شرط عبور:** قاعدهٔ مرکزی + تست‌های امنیت لینک.

## فاز ۶ — گزارش و کنترل (۱ هفته) ⬜

- [ ] داشبورد از فاز مشتق + گزارش‌های فصل ۱۱ + مرکز اعلان + پنل مانیتورینگ پیامک

**شرط عبور:** گزارش از دادهٔ واقعی تولید شود.

## فاز ۷ — تست، استقرار، تحویل (۱–۲ هفته) ⬜

- [ ] تست پذیرش با ۲۰ و ۲۰۰ رکورد + ۵۰ نشست هم‌زمان + بار ۳ برابری
- [ ] backup/restore واقعی + rollback + تست امنیت لینک
- [ ] UAT + آموزش کاربر + استقرار production

**شرط عبور:** هر ۴۷ معیار [acceptance-criteria](docs/product/acceptance-criteria.md) تأیید شود.
